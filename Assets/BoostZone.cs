using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BoostZone : MonoBehaviour
{

    //public float boostAmount = 150f;
    //public int boostDirection = 1; // -1 for left

    //// Start is called before the first frame update
    //private void OnTriggerEnter2D(Collider2D collision)
    //{
    //    if (collision.gameObject.CompareTag("Player"))
    //    {
    //        collision.gameObject.GetComponent<Movement>().GiveBoost(boostAmount, boostDirection);
    //    }
    //}
    //private void OnTriggerExit2D(Collider2D collision)
    //{
    //    if (collision.gameObject.CompareTag("Player"))
    //    {
    //        collision.gameObject.GetComponent<Movement>().RemoveBoost(boostAmount, boostDirection);
    //    }
    //}
}
